<!DOCTYPE html>
<html lang="en">
  <head>
      <%- include('../../layout/head') %>
  </head>
  <body>
    <%- include('../../layout/header') %>
    <main>
        <ul>
            <li>
              <img src=<%= book.image %>>
             <h3> <%= book.title %> </h3>
              <i><%= book.author %></i>
              <p><%= book.description %></p>
              <p><%= book.bookshelf %> </p>
            </li>
          </ul>
    </main>
</html>